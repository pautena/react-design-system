import { HeaderProps } from './header.types';
/**
 * Section used to explain give basic information about the page
 * and put the main actions
 */
export declare const Header: ({ title, loadingTitle, subtitle, loadingSubtitle, preset, actionsVariant, breadcrumbs, actions, tabs, tabsMode, navigationButton, }: HeaderProps) => import("react/jsx-runtime").JSX.Element;
