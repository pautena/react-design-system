import { Meta } from "@storybook/blocks";
import { Description } from "@storybook/addon-docs";

{/* prettier-ignore */}
<Meta title="Generators/Generators" />

# Generators

The generators are a group of components used to generate a CRUD (Create, Read, Update, Delete) interface based on a schema specification.
These components help streamline the development process by automatically creating the necessary forms, tables, and
other UI elements required for managing data entities as defined by the schema.

## ModelForm

import { ModelForm } from "../model-form/model-form";

<Description of={ModelForm} />

[ModelForm Stories](?path=/docs/generators-modelform--docs)

## ObjectDetails

import { ObjectDetails } from "../object-details/object-details";

<Description of={ObjectDetails} />

[ObjectDetails Stories](?path=/docs/generators-objectdetails--docs)

## TableList

import { TableList } from "../table-list";

<Description of={TableList} />

[TableList Stories](?path=/docs/generators-tablelist--docs)
